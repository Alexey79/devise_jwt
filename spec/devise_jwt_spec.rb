require 'spec_helper'

describe DeviseJwt do
  it 'has a version number' do
    expect(DeviseJwt::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
